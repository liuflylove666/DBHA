# DBHA shared packages

This directory contains only the `logger` and `apm/trace` packages used by DBHA v2,
copied from TencentBlueKing/blueking-dbm under the root MIT license.
The original Go module path is retained for compatibility. The root `go.work`
resolves this module locally; the BlueKing DBM platform is not required.
